'use client';

import AppsPage from '@/app/components/app';
import { ContentContainer } from '@/app/components/layout/page-container';

// 主组件
const Page = () => {
  return (
    <ContentContainer>
      <AppsPage />
    </ContentContainer>
  );
};

export default Page;