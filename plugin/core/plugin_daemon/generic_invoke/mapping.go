package generic_invoke

import "github.com/jjgagacy/workflow-app/plugin/core/session_manager"

func GetInvokePluginMap(
	session *session_manager.Session,
	request any,
) map[string]any {
	panic("not impl")
}
