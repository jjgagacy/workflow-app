export enum PluginArch {
  AMD64 = "amd64",
  ARM64 = "arm64",
}

export enum PluginLanguage {
  PYTHON = "python",
  NODE = "node",
  GO = "go",
}

export enum PluginType {
  Plugin = "plugin",
}

export enum PluginProviderType {
  Tool = "tool",
  Model = "model",
  Endpoint = "endpoint",
}


