export enum PromptMessageContentType {
  TEXT = 'text',
  IMAGE = 'image',
  AUDIO = 'audio',
  VIDEO = 'video',
  DOCUMENT = 'document',
}

export enum PromptMessageRole {
  SYSTEM = 'sytem',
  USER = 'user',
  ASSISTANT = 'assistant',
  TOOL = 'tool',
}

